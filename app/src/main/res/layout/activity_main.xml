<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">


    <RadioGroup
        android:id="@+id/main_rg"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:background="#ffffff"
        android:gravity="bottom"
        android:orientation="horizontal"
        android:paddingBottom="3.5dp">

        <RadioButton
            android:id="@+id/home_rb"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="#00666666"
            android:button="@null"
            android:drawablePadding="2dp"
            android:drawableTop="@drawable/home_selector"
            android:gravity="center_horizontal|bottom"
            android:text="首页"
            android:textColor="@drawable/home_text_selector"
            android:textSize="12sp" />

        <RadioButton
            android:id="@+id/score_rb"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="#00666666"
            android:button="@null"
            android:drawablePadding="2dp"
            android:drawableTop="@drawable/score_selector"
            android:gravity="center_horizontal|bottom"
            android:text="比分"
            android:textColor="@drawable/home_text_selector"
            android:textSize="12sp" />

        <RadioButton
            android:id="@+id/purchase_history_rb"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="#00666666"
            android:button="@null"
            android:drawablePadding="2dp"
            android:drawableTop="@drawable/purchase_history_selector"
            android:gravity="center_horizontal|bottom"
            android:text="购彩记录"
            android:textColor="@drawable/home_text_selector"
            android:textSize="12sp" />

        <RadioButton
            android:id="@+id/mine_rb"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="#00666666"
            android:button="@null"
            android:drawablePadding="2dp"
            android:drawableTop="@drawable/mine_selector"
            android:gravity="center_horizontal|bottom"
            android:text="我的"
            android:textColor="@drawable/home_text_selector"
            android:textSize="12sp" />

    </RadioGroup>


    <FrameLayout
        android:id="@+id/main_fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/main_rg" />
</RelativeLayout>