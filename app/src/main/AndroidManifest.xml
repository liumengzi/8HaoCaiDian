<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.tuding.client.eightnumcolour"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    >

    <application
        android:name=".application.RBBaseApplication"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:replace="android:allowBackup"
        >
        <activity
            android:name=".activity.LoginActivity"
            android:theme="@style/TranslucentTheme"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/TranslucentTheme"
            >
        </activity>
        <activity
            android:name=".activity.RegisterActivity"
            android:theme="@style/TranslucentTheme"
            />
        <activity
            android:name=".activity.RetrievePasswordActivity"
            android:theme="@style/TranslucentTheme"
            />
        <activity
            android:name=".activity.BetActivity"
            android:theme="@style/TranslucentTheme"
            />
        <activity
            android:name=".activity.SetNickNameAndPsActivity"
            android:theme="@style/TranslucentTheme"
            />
        <activity
            android:name=".activity.SetActivity"
            android:theme="@style/TranslucentTheme"
            />
        <activity
            android:name=".activity.LotteryResultsActivity"
            android:theme="@style/TranslucentTheme"
            />
        <activity android:name=".activity.DrawMoneyManagerActivity"
            android:theme="@style/TranslucentTheme"/>
        <activity
            android:name=".activity.ConnactServiceActivity"
            android:theme="@style/TranslucentTheme"
            />
        <activity
            android:name=".activity.FindBackPayPsActivity"
            android:theme="@style/TranslucentTheme"
            />
        <activity
            android:name=".activity.ChargeActivity"
            android:theme="@style/TranslucentTheme"
            />
    </application>

</manifest>